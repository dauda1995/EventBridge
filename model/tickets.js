export const tickets =(obj)=> {
   return { 
            ticketId: ' ',
            eventId:obj.eventId,
            customerId:obj.customerId,
            dateCreated:obj.dateCreated
            }

}